# Built packs. The release archive is generated by GitHub from the tag, so a
# zip in the working tree is always a stray local build.
*.zip

# PopTracker writes these beside a pack it has loaded from a folder.
/.pack_cache/
*.json.bak

# OS litter
.DS_Store
Thumbs.db
desktop.ini
